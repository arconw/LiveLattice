# Local Secrets

This directory is intentionally empty in git except for this README and `.gitkeep`.

Use it only for local development secrets that should never be committed. Prefer `.env.example` for documented configuration keys and keep real values in local `.env` files.
